# 🎯 Шпаргалка по тегам анимации врагов

## Быстрые примеры для копирования

### 🐺 Обычные враги
```
<appear: fade>
<idle: sway>
<death: fadeOut>
```

### 👻 Призраки
```
<appear: fade>
<idle: hover>
<death: disintegrate>
```

### 🔥 Огненные существа
```
<aura color: 255,100,0>
<appear: flash>
<idle: pulse>
<death: explode>
```

### ⚡ Электрические враги
```
<aura color: 100,100,255>
<appear: zoomIn>
<idle: shake>
<death: disintegrate>
```

### 🧊 Ледяные враги
```
<aura color: 150,200,255>
<appear: slideDown>
<idle: sway>
<death: disintegrate>
```

### 🌿 Природные враги
```
<aura color: 0,255,100>
<appear: slideUp>
<idle: sway>
<death: fadeOut>
```

### 🐉 Драконы
```
<boss aura>
<aura color: 255,0,0>
<appear: flash>
<idle: hover>
<death: explode>
<shake intensity: 20>
```

### 👑 Финальный босс
```
<boss>
<boss aura>
<aura color: 255,215,0>
<appear: flash>
<idle: pulse>
<death: explode>
<shake intensity: 20>
```

### 🤖 Роботы
```
<appear: zoomIn>
<idle: shake>
<death: disintegrate>
<shake intensity: 8>
```

### 💀 Нежить
```
<aura color: 100,0,100>
<appear: fade>
<idle: hover>
<death: disintegrate>
```

## Цвета аур (RGB)

| Цвет | Код |
|------|-----|
| 🔴 Красный | `255,0,0` |
| 🔵 Синий | `0,0,255` |
| 🟢 Зеленый | `0,255,0` |
| 🟡 Желтый | `255,255,0` |
| 🟣 Фиолетовый | `255,0,255` |
| 🔷 Голубой | `0,255,255` |
| 🟠 Оранжевый | `255,165,0` |
| 🟤 Коричневый | `139,69,19` |
| 🩷 Розовый | `255,192,203` |
| 🟡 Золотой | `255,215,0` |
| ⚫ Темно-красный | `139,0,0` |
| 🔵 Темно-синий | `0,0,139` |
| 🟢 Темно-зеленый | `0,100,0` |

## Все доступные эффекты

### Появление (appear)
- `none` - без эффекта
- `fade` - плавное появление
- `slideUp` - снизу вверх
- `slideDown` - сверху вниз
- `slideLeft` - слева направо
- `slideRight` - справа налево
- `zoomIn` - увеличение
- `spin` - вращение
- `flash` - вспышка

### Ожидание (idle)
- `none` - без анимации
- `sway` - покачивание
- `pulse` - пульсация
- `shake` - дрожание
- `hover` - левитация

### Смерть (death)
- `default` - стандартная
- `fadeOut` - исчезновение
- `fall` - падение
- `explode` - взрыв
- `disintegrate` - распад
- `spinOut` - вращение наружу

## Комбинации по типам врагов

### Слабые враги (1-10 уровень)
```
<appear: slideUp>
<idle: sway>
<death: fadeOut>
```

### Средние враги (11-30 уровень)
```
<appear: fade>
<idle: pulse>
<death: fall>
<shake intensity: 5>
```

### Сильные враги (31-50 уровень)
```
<appear: zoomIn>
<idle: hover>
<death: explode>
<shake intensity: 10>
```

### Мини-боссы
```
<boss aura>
<aura color: 200,0,200>
<appear: spin>
<idle: pulse>
<death: explode>
<shake intensity: 15>
```

### Главные боссы
```
<boss>
<boss aura>
<aura color: 255,215,0>
<appear: flash>
<idle: hover>
<death: explode>
<shake intensity: 20>
```

## Специальные теги

### Отключить все анимации
```
<no animations>
```

### Только аура без других эффектов
```
<boss aura>
<aura color: 255,0,0>
<idle: none>
<appear: none>
<death: default>
```

### Максимальная зрелищность
```
<boss aura>
<aura color: 255,215,0>
<appear: flash>
<idle: pulse>
<death: explode>
<shake intensity: 20>
```

### Минимальные эффекты (для производительности)
```
<appear: fade>
<idle: none>
<death: fadeOut>
```
